# AI Provider API Keys
# Add keys for the providers you want to use

# Google Gemini (Required for direct Gemini models)
GEMINI_API_KEY=your-gemini-api-key-here

# OpenRouter (Fallback for all other models)
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key-here

# OpenAI (Optional - for direct OpenAI API access)
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic (Optional - for direct Anthropic API access)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Mistral (Optional - for direct Mistral API access)
MISTRAL_API_KEY=your-mistral-key-here

# Cohere (Optional - for direct Cohere API access)
COHERE_API_KEY=your-cohere-key-here

# MCP Configuration
MCP_CONTAINER_NAME=kali-pentest
MCP_SERVER_PATH=/app/mcp-server/tmux-writer.js
GOTTY_WS_URL=ws://localhost:8080/ws
GOTTY_HOST=kali-pentest

# Windows MCP Configuration (for Windows projects)
# Use 'host.docker.internal' (default) or your host machine's IP address
# If host.docker.internal doesn't work, try your actual IP (e.g., 192.168.1.100)
WINDOWS_MCP_HOST=host.docker.internal

# MCP API Key Master Secret (for per-project API key generation)
# Generate a random secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This is used to generate unique API keys for each Windows project
MCP_MASTER_SECRET=your-random-secret-here-generate-with-crypto

# Server Configuration
PORT=3001
NODE_ENV=development

# Debug
DEBUG=false
