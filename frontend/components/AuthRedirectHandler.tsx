'use client'

/**
 * AuthRedirectHandler
 * 
 * Placeholder component - OAuth redirects are now handled by KeycloakCallbackHandler
 * This component is kept for backwards compatibility but does nothing
 */
export default function AuthRedirectHandler() {
  return null
}
